You are continuing development of the AI Research Agent.

IMPORTANT:

1. Read `AGENT.md` completely before writing ANY code.
2. Treat `AGENT.md` as the architecture source of truth.
3. Maintain compatibility with ALL previous phases.
4. Do NOT rewrite working infrastructure unnecessarily.
5. Do NOT simplify the distributed architecture.

---

# CURRENT OBJECTIVE

Implement PHASE 3:

# Reflection Intelligence + Memory System + RAG Infrastructure

The goal of this phase is to evolve the system from:

```text id="s24hvb"
distributed research execution
```

into:

```text id="8kxy2u"
self-improving autonomous research intelligence
```

This phase introduces:

* reflection intelligence
* long-term memory
* semantic retrieval
* advanced RAG
* hallucination detection
* research refinement loops
* persistent knowledge systems

---

# PHASE 3 GOALS

Build the intelligence layer of the platform.

The system must now:

* remember prior research
* retrieve semantic context
* improve its own outputs
* detect weak answers
* request additional research autonomously
* avoid hallucinations
* maintain long-term memory

---

# REQUIRED IMPLEMENTATIONS

---

# 1. Advanced Reflection/Critic System

Upgrade the Reflection Agent into a true autonomous critic system.

The Reflection Agent must:

* evaluate report quality
* detect missing information
* detect hallucinations
* verify citations
* detect weak reasoning
* request additional research
* score confidence levels

---

## Reflection Output Schema

Implement structured outputs:

```python id="w6d2kk"
class ReflectionResult(TypedDict):
    requires_additional_research: bool
    missing_topics: List[str]
    hallucination_risk: float
    confidence_score: float
    reasoning_quality: float
    citation_issues: List[str]
    recommended_actions: List[str]
```

---

# 2. Reflection Loop Intelligence

Implement intelligent research refinement loops.

Flow:

```text id="llvuwv"
Research
   ↓
Draft
   ↓
Reflection
   ↓
Weak Areas Detected?
   ↓ YES
Generate New Tasks
   ↓
Research Again
```

The Reflection Agent must generate:

* new search queries
* missing research tasks
* targeted retrieval instructions

---

# 3. Reflection Safety Enforcement

Implement hard safety protection.

The orchestrator MUST enforce:

```python id="yrq5jl"
reflection_count >= max_reflections
```

The loop MUST terminate safely.

Add:

* loop protection
* token protection
* timeout protection
* emergency stop safeguards

Infinite loops must be impossible.

---

# 4. Long-Term Memory Architecture

Implement persistent memory systems.

Create:

```text id="a3n7mn"
memory/
├── episodic/
├── semantic/
├── retrieval/
├── compression/
├── vector_store/
└── summarization/
```

---

## Memory Types

### Episodic Memory

Store:

* sessions
* workflows
* past reports
* user interactions

### Semantic Memory

Store:

* embeddings
* extracted knowledge
* reusable insights

### Compressed Memory

Store:

* summarized historical context
* condensed research history

---

# 5. Vector Database Layer

Implement vector abstraction layer.

Architecture MUST support:

```text id="vst2xz"
Development:
- ChromaDB

Production:
- Qdrant
```

Implement provider abstraction:

```python id="jlwmce"
class VectorStoreProvider:
    async def add_documents(...)
    async def similarity_search(...)
    async def delete(...)
```

The rest of the platform MUST NOT depend directly on ChromaDB.

---

# 6. RAG Pipeline

Implement full production-grade RAG.

Pipeline:

```text id="v8v0dx"
Document Upload
      ↓
Chunking
      ↓
Embedding Generation
      ↓
Metadata Extraction
      ↓
Vector Storage
      ↓
Retriever
      ↓
Context Injection
```

---

# 7. Document Processing Infrastructure

Implement support for:

* PDFs
* Markdown
* text files
* GitHub READMEs
* HTML pages

Add:

* chunking strategies
* metadata extraction
* semantic indexing
* document deduplication

---

# 8. Embedding Infrastructure

Implement embedding layer.

Requirements:

* async embedding generation
* batch embeddings
* retry handling
* caching
* provider abstraction

Recommended models:

* nomic-embed-text
* bge-large

---

# 9. Memory Retrieval System

Implement semantic retrieval engine.

The system must retrieve:

* relevant past sessions
* prior findings
* reusable research
* contextual knowledge

Retrieval should support:

* similarity search
* metadata filtering
* hybrid retrieval

---

# 10. Context Compression System

Implement memory compression.

Purpose:

* reduce token usage
* preserve useful history
* maintain long conversations

Implement:

* summarization pipelines
* rolling context windows
* compressed memory snapshots

---

# 11. Citation Verification System

Upgrade Citation Agent.

Requirements:

* source validation
* duplicate detection
* broken citation detection
* hallucinated citation detection

The Reflection Agent must validate:

* every citation exists
* claims are grounded in sources

---

# 12. Research Confidence Scoring

Implement confidence scoring system.

Track:

* evidence quality
* source credibility
* citation coverage
* hallucination risk

Store inside state:

```python id="d4r2t2"
metadata["confidence"]
```

---

# 13. Knowledge Reuse System

Implement reusable research memory.

The platform should:

* reuse previous findings
* avoid duplicate research
* accelerate repeated workflows

Example:

```text id="6vxcf9"
Previous LangGraph research exists
    ↓
Reuse prior semantic memory
    ↓
Avoid unnecessary web research
```

---

# 14. Hybrid Retrieval Pipeline

Implement:

* semantic retrieval
* keyword retrieval
* metadata filtering
* reranking

The retriever should support:

* high recall
* relevance scoring
* contextual reranking

---

# 15. RAG Worker Infrastructure

Create dedicated RAG workers.

Queues:

```text id="yvcm3g"
rag_embeddings
rag_indexing
rag_retrieval
memory_compression
```

These must integrate with Celery.

---

# 16. Reflection Observability

Track:

* reflection cycles
* hallucination rates
* confidence scores
* memory retrieval quality
* token usage growth

Expose metrics through:

* Prometheus
* LangSmith
* structured logs

---

# 17. Integration Tests

Add tests for:

## Reflection

* loop termination
* hallucination detection
* task regeneration

## RAG

* indexing
* retrieval
* reranking

## Memory

* persistence
* retrieval
* compression

## Vector Store

* add/search/delete
* provider abstraction compatibility

---

# 18. API Enhancements

Add endpoints for:

```text id="t2hn4j"
POST /documents/upload
GET  /memory/search
GET  /memory/session/{id}
POST /research/retry
```

Support:

* async uploads
* streaming indexing progress

---

# 19. Docker Infrastructure Upgrades

Add services:

```yaml id="sm26j3"
chromadb:
qdrant:
rag-worker:
memory-worker:
```

All services must:

* support health checks
* restart automatically
* expose metrics

---

# 20. Performance Requirements

The memory + RAG layer must support:

* large document collections
* concurrent retrieval
* fast semantic search
* scalable indexing
* multi-session memory

---

# REQUIRED IMPLEMENTATION RULES

---

## Reflection Rules

The Reflection Agent MUST:

* use structured outputs
* never loop infinitely
* generate actionable improvements

---

## RAG Rules

The system MUST:

* support provider abstraction
* avoid vendor lock-in
* support future Qdrant migration

---

## Memory Rules

Memory must be:

* persistent
* queryable
* compressible
* reusable

---

## Architecture Rules

Do NOT:

* hardcode vector DB dependencies
* bypass memory abstraction
* store raw unbounded chat history
* allow infinite reflection loops

---

# OUTPUT FORMAT

For EVERY implementation:

1. Explain what is being built
2. Explain WHY it exists architecturally
3. Generate production-grade code
4. Use correct file paths
5. Ensure imports work
6. Ensure async compatibility
7. Ensure Docker compatibility
8. Ensure test compatibility

---

# VALIDATION REQUIREMENTS

Before finishing:

Verify:

* reflection loops terminate correctly
* RAG indexing works
* retrieval works
* vector DB abstraction works
* memory persistence works
* confidence scoring works
* citations validate correctly
* integration tests pass
* Docker services boot

---

# FINAL DELIVERABLE

At the end of Phase 3:

The platform should behave like:

```text id="fwy7zx"
an autonomous self-improving research intelligence system
with persistent semantic memory and advanced retrieval
```

Then STOP.

Do NOT continue to Phase 4 automatically.
