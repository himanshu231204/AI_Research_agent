You are now responsible for building the COMPLETE production-grade CI/CD and GitHub workflow system for this project.

You MUST follow the architecture defined in `AGENT.md`.

This is NOT a demo repository.

The repository must behave like a real enterprise AI platform with strict quality gates.

---

# PRIMARY GOAL

Create a professional CI/CD pipeline so that:

* every commit is validated
* every PR is automatically tested
* formatting violations fail CI
* typing violations fail CI
* broken imports fail CI
* Docker build failures fail CI
* LangGraph execution failures fail CI
* Celery startup failures fail CI

The pipeline must protect the repository from bad code.

---

# REQUIRED STACK

Build CI/CD using:

* GitHub Actions
* pytest
* ruff
* black
* mypy
* docker compose
* pre-commit
* coverage.py

---

# REQUIRED WORKFLOWS

Create these workflows:

## 1. ci.yml

Runs on:

* pull_request
* push to main/dev

Must execute:

### Python Validation

* install dependencies
* verify lockfile
* verify imports

### Linting

* ruff
* black --check

### Type Checking

* mypy

### Unit Tests

* pytest

### Coverage

* coverage report

### LangGraph Validation

* ensure graph imports
* ensure graph compiles

### Celery Validation

* ensure Celery app boots

### Docker Validation

* docker compose config
* build containers

### Environment Validation

* verify required env vars exist

---

## 2. security.yml

Run:

* dependency vulnerability scans
* pip-audit
* secret scanning

---

## 3. docker.yml

Build and validate:

* api image
* worker image
* browser sandbox image

Push-ready architecture only.
Do NOT actually deploy yet.

---

## 4. release.yml

Prepare semantic version releases.

Must support:

* tagged releases
* changelog generation
* GitHub release artifacts

---

# REQUIRED TESTING ARCHITECTURE

Create proper test structure:

```text
tests/
├── unit/
├── integration/
├── graph/
├── api/
├── workers/
└── e2e/
```

---

# REQUIRED TESTS

Implement foundational tests for:

## LangGraph

* graph compilation
* node registration
* conditional routing
* reflection loop limits

## API

* health endpoint
* websocket connection
* config loading

## Celery

* worker boot
* queue registration
* retry config

## Ollama Layer

* client initialization
* timeout handling
* fallback routing preparation

## State Schema

Validate:

* required fields
* reflection_count logic
* max_reflections enforcement

---

# REQUIRED QUALITY RULES

The repository MUST enforce:

## Formatting

Use:

* black
* ruff

---

## Typing

Use:

* strict mypy mode

No untyped functions.

---

## Import Rules

* no circular imports
* no wildcard imports

---

## Logging Rules

* structured logging only
* no print() statements

---

# REQUIRED PRE-COMMIT SETUP

Configure pre-commit hooks for:

* black
* ruff
* mypy
* trailing whitespace
* yaml validation
* large file detection

Developers should NOT be able to commit broken code locally.

---

# REQUIRED PROJECT FILES

Create:

* .github/workflows/ci.yml
* .github/workflows/security.yml
* .github/workflows/docker.yml
* .github/workflows/release.yml
* pyproject.toml
* .pre-commit-config.yaml
* pytest.ini
* .env.example
* Makefile

---

# REQUIRED MAKE COMMANDS

Implement:

```bash
make test
make lint
make format
make typecheck
make docker-build
make run-api
make run-worker
make run-dev
```

---

# IMPORTANT IMPLEMENTATION RULES

## CI FAILURE RULE

If ANY of these fail:

* tests
* typing
* formatting
* imports
* Docker build
* graph validation

The CI pipeline MUST fail.

---

## LANGGRAPH RULES

The CI pipeline must validate:

```python
graph.compile()
```

This prevents broken graph states from merging into main.

---

## CELERY RULES

The pipeline must validate:

* worker boot
* Redis connection config
* queue registration

---

## DOCKER RULES

The pipeline must validate:

```bash
docker compose build
```

for:

* api
* worker
* browser sandbox

---

# OUTPUT FORMAT

For every file:

1. Explain WHY it exists
2. Explain what production problem it solves
3. Generate full file content
4. Use correct paths
5. Ensure syntax correctness

---

# FINAL GOAL

At the end:

A developer should be able to:

```bash
git clone ...
make run-dev
```

AND:

* all services boot
* tests pass
* CI passes
* Docker builds
* graph compiles
* Celery workers start

without manual fixes.

---

# CRITICAL REQUIREMENT

This repository MUST feel like:

* a real startup backend
* enterprise-grade infrastructure
* production AI platform

NOT:

* a tutorial repo
* notebook project
* toy AI demo
