You are continuing development of the AI Research Agent.

IMPORTANT:

1. Read `AGENT.md` completely before writing ANY code.
2. Treat `AGENT.md` as the architecture source of truth.
3. Maintain compatibility with ALL previous phases.
4. Do NOT simplify the distributed architecture.
5. Preserve MCP abstractions and worker isolation.
6. Do NOT rewrite working systems unnecessarily.

---

# CURRENT OBJECTIVE

Implement PHASE 5:

# Hybrid LLM Routing + Multi-Model Intelligence + Cost Optimization

The goal of this phase is to evolve the platform from:

```text id="9n8m5j"
tool-using autonomous system
```

into:

```text id="w7a2k1"
multi-model autonomous intelligence platform
```

This phase introduces:

* hybrid local/cloud routing
* dynamic model selection
* intelligent fallback systems
* token optimization
* model specialization
* GPU-aware scheduling
* cost-aware orchestration
* adaptive inference strategies

---

# PHASE 5 GOALS

Build a production-grade model orchestration layer.

The system must now:

* intelligently choose models
* fallback automatically on failures
* optimize latency and cost
* route workloads dynamically
* support multiple providers
* monitor GPU pressure
* support local-first execution
* degrade gracefully under load

---

# REQUIRED IMPLEMENTATIONS

---

# 1. Multi-Model Routing Architecture

Create centralized model routing layer.

Implement:

```text id="g9u2vf"
models/
├── routing/
├── providers/
├── local/
├── cloud/
├── policies/
├── telemetry/
└── fallbacks/
```

The routing layer must:

* support multiple providers
* support automatic failover
* support cost-aware selection
* support workload specialization

---

# 2. Provider Abstraction Layer

Implement provider abstraction.

Create:

```python id="h2x7jc"
class LLMProvider:
    async def generate()
    async def stream()
    async def embeddings()
    async def health_check()
```

The rest of the platform MUST NEVER directly depend on:

* OpenAI SDK
* Anthropic SDK
* Ollama SDK

All access must go through provider abstraction.

---

# 3. Local Model Infrastructure

Implement robust Ollama integration.

Requirements:

* model health checks
* timeout handling
* GPU monitoring
* model availability checks
* streaming support
* concurrent request handling

Supported local models:

* qwen3
* llama3
* mistral
* deepseek-coder

---

# 4. Cloud Model Integration

Implement cloud providers.

Support:

* GPT-5
* Claude
* Gemini

Requirements:

* async clients
* retries
* timeout handling
* rate limiting
* usage tracking

The architecture MUST support future providers easily.

---

# 5. Hybrid Fallback System

Implement automatic fallback routing.

Flow:

```text id="r2v7kw"
Try Local Model
      ↓ FAIL
Cloud Fallback
      ↓ FAIL
Secondary Fallback
```

Fallback conditions:

* timeout
* GPU saturation
* unavailable model
* queue overload
* provider failure

The fallback system must:

* retry intelligently
* preserve workflow continuity
* log routing decisions

---

# 6. Dynamic Model Selection

Implement intelligent model routing.

The system should route based on:

* task complexity
* latency requirements
* token budget
* workload type
* GPU availability

Example:

```text id="y0pw8w"
Planning
    → qwen3

Code Analysis
    → deepseek-coder

Fast Reflection
    → mistral

High-accuracy fallback
    → GPT-5
```

---

# 7. Cost Optimization Engine

Implement token + cost optimization.

Track:

* token usage
* estimated costs
* provider costs
* workflow costs
* per-agent costs

Store inside:

```python id="q1jj5z"
state["token_usage"]
state["metadata"]["costs"]
```

---

# 8. GPU-Aware Scheduling

Implement GPU-aware inference routing.

Monitor:

* VRAM usage
* queue latency
* active inference jobs
* model load

The router should:

* avoid overloaded models
* distribute inference intelligently
* fallback when GPUs are saturated

---

# 9. Streaming Inference Layer

Implement unified streaming system.

Support:

* token streaming
* partial outputs
* async streaming
* cancellation support

The API should support:

* live streaming responses
* streaming across providers

---

# 10. Model Health Monitoring

Implement health system.

Track:

* response latency
* timeout frequency
* provider uptime
* token throughput
* GPU load

Expose metrics through:

* Prometheus
* Grafana
* LangSmith

---

# 11. Intelligent Retry Policies

Implement provider-aware retries.

Requirements:

* exponential backoff
* provider switching
* failure categorization
* circuit breakers

The platform MUST avoid:

* retry storms
* cascading failures

---

# 12. Circuit Breaker System

Implement production-grade circuit breakers.

If a provider repeatedly fails:

* temporarily disable provider
* reroute traffic
* periodically retry health checks

This prevents:

* system-wide degradation
* repeated timeout chains

---

# 13. Prompt Routing Intelligence

Implement prompt-aware routing.

The system should detect:

* coding tasks
* summarization tasks
* reasoning tasks
* retrieval tasks

and route intelligently.

---

# 14. Adaptive Token Budgeting

Implement token management.

Capabilities:

* max token enforcement
* prompt compression
* memory summarization
* context trimming

Prevent:

* runaway token growth
* oversized prompts

---

# 15. Model Telemetry System

Track:

* tokens/sec
* latency
* routing decisions
* fallback frequency
* GPU usage
* provider costs

Every request should include:

* provider
* model
* latency
* token count
* fallback chain

---

# 16. API Enhancements

Add endpoints for:

```text id="8m8dbb"
GET /models/status
GET /models/providers
POST /models/test
GET /telemetry/models
GET /telemetry/costs
```

Support:

* real-time monitoring
* provider diagnostics

---

# 17. Distributed Inference Workers

Create dedicated inference workers.

Queues:

```text id="2pmv5h"
local_inference
cloud_inference
embeddings
reflection_models
```

Workers must:

* support retries
* support provider failover
* expose health metrics

---

# 18. Integration Tests

Add tests for:

## Routing

* local routing
* cloud fallback
* retry logic

## Providers

* provider abstraction compatibility
* streaming support

## Cost Tracking

* token accounting
* aggregation correctness

## Circuit Breakers

* provider shutdown
* automatic recovery

---

# 19. Docker Infrastructure Upgrades

Add services:

```yaml id="3a8m0n"
ollama:
model-router:
inference-worker:
telemetry-worker:
```

All services must:

* support health checks
* expose metrics
* restart automatically

---

# 20. Performance Requirements

The model routing layer must support:

* concurrent inference
* automatic failover
* low-latency routing
* scalable inference workers
* local-first execution
* graceful degradation

---

# REQUIRED IMPLEMENTATION RULES

---

## Routing Rules

The system MUST:

* prefer local models first
* fallback automatically
* avoid provider lock-in

---

## Cost Rules

The platform MUST:

* track all token usage
* estimate costs accurately
* expose cost telemetry

---

## Reliability Rules

The platform MUST:

* survive provider outages
* reroute automatically
* avoid cascading failures

---

## Architecture Rules

Do NOT:

* hardcode providers
* tightly couple SDKs
* bypass routing abstractions
* disable fallback handling

---

# OUTPUT FORMAT

For EVERY implementation:

1. Explain what is being built
2. Explain WHY it exists architecturally
3. Generate production-grade code
4. Use correct file paths
5. Ensure imports work
6. Ensure async compatibility
7. Ensure Docker compatibility
8. Ensure test compatibility

---

# VALIDATION REQUIREMENTS

Before finishing:

Verify:

* local routing works
* cloud fallback works
* streaming works
* GPU-aware routing works
* circuit breakers work
* token tracking works
* provider abstraction works
* integration tests pass
* Docker services boot

---

# FINAL DELIVERABLE

At the end of Phase 5:

The platform should behave like:

```text id="dr4s6t"
a resilient multi-model autonomous AI platform
capable of intelligent routing, hybrid inference,
automatic fallback, and production-grade cost optimization
```

Then STOP.

Do NOT continue to Phase 6 automatically.
