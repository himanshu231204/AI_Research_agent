'use client';

import { ModelDashboard } from '@/features/models/model-dashboard';

export default function ModelsPage() {
  return (
    <div className="h-full">
      <ModelDashboard />
    </div>
  );
}