You are continuing development of the AI Research Agent.

IMPORTANT:

1. Read `AGENT.md` completely before writing ANY code.
2. Treat `AGENT.md` as the architecture source of truth.
3. Maintain compatibility with ALL previous phases.
4. Do NOT simplify the distributed architecture.
5. Do NOT rewrite working systems unnecessarily.
6. Browser execution MUST remain isolated and sandboxed.

---

# CURRENT OBJECTIVE

Implement PHASE 4:

# MCP Integration + Browser Automation + Tool Ecosystem

The goal of this phase is to evolve the platform from:

```text id="m1v7sz"
autonomous research intelligence
```

into:

```text id="u93g2v"
tool-using autonomous AI Research Agent
```

This phase introduces:

* MCP server integration
* browser automation
* sandboxed execution
* dynamic tool usage
* GitHub analysis tools
* filesystem tools
* terminal execution
* autonomous browsing
* tool orchestration

---

# PHASE 4 GOALS

Build a production-grade autonomous tool ecosystem.

The system must now:

* dynamically use tools
* browse websites autonomously
* analyze GitHub repositories
* interact with filesystems
* execute terminal commands safely
* support MCP protocol servers
* run browser automation securely
* orchestrate tools across distributed workers

---

# REQUIRED IMPLEMENTATIONS

---

# 1. MCP Architecture

Implement full MCP integration layer.

Create:

```text id="9u8wdw"
mcp/
├── client/
├── registry/
├── servers/
├── adapters/
├── schemas/
└── transport/
```

The MCP layer must support:

* dynamic server registration
* async communication
* structured tool schemas
* tool discovery
* retry handling
* timeout protection

---

# 2. MCP Client System

Build a reusable MCP client manager.

Requirements:

* async communication
* connection pooling
* health monitoring
* reconnect logic
* capability discovery

Implement:

```python id="rwq3yb"
class MCPClientManager:
    async def connect()
    async def list_tools()
    async def execute_tool()
    async def health_check()
```

---

# 3. MCP Registry

Implement centralized tool registry.

The system must:

* dynamically register tools
* expose tool metadata
* support routing by capability
* support permissions

Example:

```text id="jwvjrx"
GitHub tools
Browser tools
Filesystem tools
Terminal tools
PDF tools
```

---

# 4. Browser Automation Infrastructure

Build production-grade browser automation.

Requirements:

* Playwright integration
* isolated browser workers
* session management
* screenshot support
* page extraction
* autonomous navigation

The Browser Agent must:

* navigate websites
* extract content
* interact with forms
* capture screenshots
* detect failures

---

# 5. Browser Sandbox Isolation (CRITICAL)

The browser MUST run in isolated containers.

Implement:

```text id="0w0x0f"
api container
    ↓
internal network
    ↓
sandboxed browser container
```

Security requirements:

* isolated Docker network
* no shared filesystem
* ephemeral browser sessions
* restricted privileges
* egress-only access

The API process MUST NEVER execute Playwright directly.

---

# 6. Browser Worker Pool

Create dedicated browser worker infrastructure.

Queues:

```text id="3s1pxh"
browser_navigation
browser_scraping
browser_interaction
browser_screenshots
```

Workers must support:

* retries
* timeout recovery
* session cleanup
* crash recovery

---

# 7. GitHub MCP Integration

Implement GitHub repository analysis.

Capabilities:

* repository cloning
* README parsing
* architecture extraction
* dependency analysis
* codebase summarization
* issue analysis

The GitHub Agent should:

* analyze repository structure
* detect technologies
* summarize architecture
* extract setup instructions

---

# 8. Filesystem MCP Integration

Implement secure filesystem operations.

Capabilities:

* file reading
* file writing
* directory traversal
* artifact generation

Security requirements:

* sandboxed paths
* restricted directories
* path validation

---

# 9. Terminal MCP Integration

Implement safe terminal execution.

Capabilities:

* shell commands
* build commands
* code execution
* repo setup commands

Security requirements:

* isolated execution
* restricted commands
* timeout enforcement
* output capture

The terminal system MUST:

* prevent dangerous execution
* avoid unrestricted shell access

---

# 10. Tool Invocation Layer

Implement unified tool execution abstraction.

Create:

```python id="qpl6gk"
class ToolExecutor:
    async def execute()
    async def validate_permissions()
    async def retry()
```

The system should support:

* retries
* observability
* structured outputs
* tracing

---

# 11. Autonomous Tool Selection

Upgrade Router Agent.

The Router Agent must now:

* select tools dynamically
* choose MCP servers
* select browser actions
* determine execution strategy

Example:

```text id="r7u3jc"
Need GitHub analysis
    ↓
Select GitHub MCP tools

Need dynamic website
    ↓
Select Browser MCP tools
```

---

# 12. Browser Intelligence Layer

Implement autonomous browsing intelligence.

Capabilities:

* detect page types
* wait for dynamic content
* retry failed navigation
* extract structured content

Add:

* content extraction pipelines
* HTML cleaning
* readability parsing

---

# 13. Screenshot + Artifact System

Implement artifact generation.

Support:

* screenshots
* HTML snapshots
* markdown exports
* extracted PDFs

Store artifacts:

* locally
* in object storage abstraction

---

# 14. Tool Observability

Track:

* tool latency
* tool failures
* retries
* browser crashes
* MCP connectivity
* execution traces

Expose metrics through:

* Prometheus
* LangSmith
* structured logs

Every tool execution must contain:

* session_id
* tool_name
* worker_id
* execution_time

---

# 15. Distributed Tool Workers

Create dedicated workers:

```text id="byglxv"
github-worker
browser-worker
filesystem-worker
terminal-worker
```

Each worker:

* isolated responsibility
* dedicated queues
* restart policies
* health checks

---

# 16. API Enhancements

Add endpoints for:

```text id="5e3z3l"
POST /browser/navigate
POST /github/analyze
POST /tools/execute
GET  /tools/list
GET  /browser/screenshot/{id}
```

Support:

* async execution
* streaming progress
* task polling

---

# 17. Browser Session Management

Implement:

* browser session tracking
* session expiration
* cookie isolation
* concurrent sessions

Support:

* multiple autonomous workflows simultaneously

---

# 18. Integration Tests

Add tests for:

## Browser

* navigation
* screenshots
* extraction
* retries

## MCP

* server registration
* tool discovery
* tool execution

## GitHub

* repository analysis
* README extraction

## Terminal

* safe command execution
* timeout handling

---

# 19. Docker Infrastructure Upgrades

Add services:

```yaml id="dq9xra"
browser-sandbox:
github-worker:
filesystem-worker:
terminal-worker:
mcp-gateway:
```

All services must:

* support health checks
* expose metrics
* restart automatically

---

# 20. Performance Requirements

The tool ecosystem must support:

* concurrent browser sessions
* distributed tool execution
* autonomous navigation
* scalable worker pools
* resilient MCP communication

---

# REQUIRED IMPLEMENTATION RULES

---

## Browser Rules

Browser execution MUST:

* remain isolated
* never run inside API
* support retries
* clean sessions automatically

---

## MCP Rules

The system MUST:

* support dynamic tool registration
* avoid hardcoded tool logic
* support future MCP servers

---

## Security Rules

The system MUST:

* sandbox execution
* restrict filesystem access
* isolate browser execution
* enforce command restrictions

---

## Architecture Rules

Do NOT:

* run Playwright inside FastAPI
* bypass MCP abstractions
* hardcode tool routing
* allow unrestricted shell access

---

# OUTPUT FORMAT

For EVERY implementation:

1. Explain what is being built
2. Explain WHY it exists architecturally
3. Generate production-grade code
4. Use correct file paths
5. Ensure imports work
6. Ensure async compatibility
7. Ensure Docker compatibility
8. Ensure test compatibility

---

# VALIDATION REQUIREMENTS

Before finishing:

Verify:

* MCP servers connect correctly
* browser workers function
* screenshots generate correctly
* GitHub analysis works
* tool routing works
* browser isolation works
* integration tests pass
* Docker services boot

---

# FINAL DELIVERABLE

At the end of Phase 4:

The platform should behave like:

```text id="4mlccs"
a production-grade autonomous tool-using AI operating system
capable of browser automation, repository analysis,
and distributed MCP-based tool orchestration
```

Then STOP.

Do NOT continue to Phase 5 automatically.
